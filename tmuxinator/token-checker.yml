name: token-checker-cms
root: ~/Workspace/crypto/token-checker

windows:
  - editor: nvim
  - bash: git fetch --all && git status
  - lint: print -S "npm run lint:watch"
  - server: print -S "npm run start"
