name: airon
root: ~/Workspace/nvim/airon.nvim
pre_window: nvm use

windows:
  - editor: nvim
  - bash: git fetch --all
  - plugin: print -S "tail -f ./airon.log"
  - agent: print -S "npm run start:agent:openai"
