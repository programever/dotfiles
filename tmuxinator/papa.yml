name: papa
root: ~/Workspace/papa
pre_window: nvm use

windows:
  - editor: nvim
  - bash1: git fetch --all && git status
  - bash2: print -S "npm run external:start && npm run external:db:migrate"
  - apiWeb: print -S "npm run start"
  - mobile: print -S "npm run start:mobile" 
